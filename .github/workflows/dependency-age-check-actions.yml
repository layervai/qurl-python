# Dependency Age Check (GitHub Actions pins)
# Thin shim calling shared reusable workflow.
# Source: https://github.com/layervai/ops-routines/blob/main/.github/workflows/age-check-actions.yml
# Implementation: https://github.com/layervai/ops-routines/blob/main/lib/age-check-actions.sh
name: Dependency Age Check (Actions)

# Fires on every PR (no path filter) so the status check always reports.
# This lets branch protection require it without the path-filtered-
# required-check footgun. The reusable's script exits 0 quickly when
# no workflow/composite-action changes are in the diff.
on:
  pull_request:
  workflow_dispatch:

# Cancel superseded runs on the same PR branch — the reusable workflow
# makes sequential registry calls; no reason to let two race for an
# outdated diff.
concurrency:
  group: dep-age-actions-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  age-check:
    # Bypass is handled inside the reusable workflow's inner job, NOT
    # here. The required status check context is
    # `age-check / Check GitHub Actions pin ages` (caller-job /
    # reusable-inner-job). If we skip at this outer level, the reusable
    # is never invoked, the inner job never reports, and the required
    # context sits at "Expected — Waiting for status" forever.
    # Letting the outer job always run means the inner job's own
    # bypass `if:` reports `conclusion: skipped`, which satisfies the
    # required status check.
    # Note: `timeout-minutes` is intentionally NOT set here. Per GH
    # docs (https://docs.github.com/en/actions/using-workflows/reusing-workflows#supported-keywords-for-jobs-that-call-a-reusable-workflow)
    # only if/name/needs/permissions/secrets/strategy/uses/with/concurrency
    # are valid on a job that calls a reusable workflow. The reusable
    # workflow itself sets `timeout-minutes: 5` internally.
    uses: layervai/ops-routines-workflows/.github/workflows/age-check-actions.yml@3f4ce1fdeb5d2f6f66b0b2c7d0ba42efce7212bc # v0.2.0
    with:
      min_age_days: 7
    permissions:
      contents: read
